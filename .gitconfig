[user]
	name = Itaraju Brum
	email = itaraju@ctc.com.br
[core]
[diff]
	tool = meld
[difftool]
	prompt = false
[merge]
	tool = meld
[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
[alias]
	tree = log --oneline --graph --all
	wdiff = diff --color-words='[^[:space:]]|([[:alnum:]]|UTF_8_GUARD)+'
        s = status
